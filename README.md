# Assignment-CO528_Angle-selector
Create an angle selector.  It must have a text box and a slider which have range of values from 0 to 360. There should be a set of 5 radio buttons that should be there to select common values (0, 45, 60, 90, 180).  The slider, radio buttons and text box should be in sync.
